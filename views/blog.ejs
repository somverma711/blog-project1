<%- include('./header.ejs') %>

<section class="blog-list mtb-40" style="margin-top: 7.6rem;" >
    <div class="container">
      <h1>Blogs</h1>
      <div class="row">
        <% b.forEach((item)=>{ %>
          <div class="col-md-4">
            <div class="blog-box">
              <img src="<%=  item.image.url %>" height="200px" alt="blog1" />
              <h3><%= item.title %></h3>
              <p><%= item.createdAt.toLocaleDateString() %></p>
              <p><%= item.description.slice(0,100) %></p>
              <a href="/blogdetail/<%= item._id %>" class="btn btn-warning">Read more. </a>
            </div>
          </div>
       <% }) %>
      
    
      </div>
   
  </section>

  <%- include('./footer.ejs') %>